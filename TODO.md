# 待办清单

> 明天要处理的三件事，按优先级排序。

---

## ① 微信推送接入（明天做）

**目标**：让朋友也能收到信号（微信渠道）

### 方案选择
推荐 **企业微信机器人**（不需要企业认证，个人就能用，免费，腾讯官方接口稳定）。

### 准备步骤（用户操作）
1. 手机下载"企业微信"App，用个人微信登录
2. 新建一个群（可以只自己一个人，或者拉朋友进群）
3. 群设置 → 群机器人 → 添加机器人 → 取名 → 完成
4. 复制 Webhook 地址，格式如：
   ```
   https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
   ```
5. 把 Webhook URL 发给 Kiro 即可

### 代码改动
- 在配置区新增 `WX_WEBHOOK_URL`
- 新增 `send_wx(message)` 函数（支持 Markdown 格式）
- 在 `scan_markets` 中推送信号时**同时发 TG 和微信**（双渠道备份）
- 消息格式要适配企业微信 Markdown（`<b>` 改成 `**`）

### 预计改动量
- ~30 行代码
- 5~10 分钟改完

---

## ② 扫描速度优化（明天做）

**目标**：在不影响 Binance API（不触发限流）的前提下，进一步提速

### 现状
- 1h 周期扫 200 个合约约 **25 秒**
- 受限于 `SCAN_WORKERS = 10` 并发数
- Binance U 本位合约的 REST 限制是 **2400 请求/分钟**

### 优化方向

#### 方向 A：提升并发数（最直接）
- `SCAN_WORKERS = 10` → 调到 `20`
- 理论扫描时间：25 秒 → 约 13 秒
- 风险：并发 20 还是远低于 Binance 限流阈值，安全

#### 方向 B：批量获取 K 线（最高效）
- 目前是**每个合约独立调 `fetch_ohlcv`**，200 次请求
- 可以改用 **Binance 官方 `klines` 接口 + 批量并发**
- 配合 `asyncio + aiohttp` 可以 5 秒内扫完

#### 方向 C：改为 WebSocket 订阅（终极）
- 订阅所有合约的 K 线流，K 线一收盘就秒出信号
- 延迟从 5 分钟 → < 1 秒
- 代码改动最大，复杂度翻倍

### 推荐方案
先试 **方向 A**（改一行 `SCAN_WORKERS = 20`）+ **关闭 enableRateLimit 的冗余 sleep**，
预计能压到 10~15 秒，够用了。

方向 B/C 留作后续升级。

---

## ③ TV 上看不到的"幽灵背离"排查（明天做）

**现象**：部分推送出来的背离，在 TradingView 图表上用官方 Quantum Flow Cipher 指标**对不上**。

### 可能原因（从高到低）

#### 1. 时间戳对齐问题（最可能）
- TV 显示的是你的**本地时区** K 线
- 脚本用的是 **UTC** 时间
- 你对比时可能看错了 K 线位置
- **排查**：TG 消息里已有 UTC 时间，在 TV 上切换到 UTC 时区再对比

#### 2. 数据源微差
- 脚本从 CCXT → Binance REST 接口拉数据
- TV 可能从 Binance 官方订阅流拉数据
- **极少数情况下**（分钟级 K 刚收盘时）两边数据可能有秒级差异
- **排查**：等 K 线完全收盘 5 分钟后再对比

#### 3. Pine Script 实现差异
- TV 上的 Quantum Flow Cipher 如果用了 `ta.pivothigh` 的**实时模式**，
  会在更早的 bar 就画线，和我们"收盘确认"逻辑不一致
- **排查**：如果 TV 用的是"repaint"模式的背离标注，那和我们不可能完全对齐

#### 4. 指标参数对不上
- 如果 TV 上用的 Quantum Flow Cipher 是改版的（参数不一样），
  那判定阈值 `F_MD=3.0`、`M_MD=3.0` 也会不同
- **排查**：在 TV 上打开 Quantum Flow Cipher 设置，对比核心参数：
  - Fusion 背离：左枢轴=5，右枢轴=3，最小间距=8，幅度阈值=3.0
  - Flow 背离：左枢轴=10，右枢轴=6，最小间距=20，幅度阈值=3.0

### 明天的排查步骤
1. 下次收到"对不上"的推送时，**截屏 TG 消息 + TV 同时间段截图** 发给 Kiro
2. 具体告诉：什么币、什么周期、TG 说的确认时间、TV 上你看到的是什么
3. 有这三份数据，基本能定位到是上面 4 种里的哪种

### 可以先做的事
- 在 `detect_divergence_signals` 里加一个 debug 模式：
  把每次**检测到但未发送**（因为过期/去重）的背离也写到本地日志
- 方便后续回查"这个币这个时间到底有没有被识别出来"

---

## 数据观察期（这几天做）

跑着系统，关注两份文件：

1. **signals.log**（CSV）
   - Excel 打开，按 `timeframe`、`sig_type`、`strength_stars` 透视
   - 看每天出几条、哪种信号最多
   
2. **sent_signals.json**
   - 不用手动看
   - 超过 1000 条了就是正常的，脚本会自动清理 3 天前的

跑几天后有数据，找 Kiro 一起调：
- `TOP_N` 是 200 合适还是要改
- `FRESH_BARS` 的分周期配置要不要微调
- 单周期是不是要砍掉（比如 15m 噪音太多）
- 强度星级分布是不是合理

---

## 优先级快速决策

| 事项 | 耗时 | 收益 | 顺序 |
|---|---|---|---|
| ① 微信机器人 | 10 分钟 | 朋友能收到 | 先做 |
| ② 扫描提速（方向 A） | 5 分钟 | 速度 ×2 | 第二 |
| ③ 排查幽灵背离 | 取决于数据 | 信号可信度 | 收到对不上的信号再看 |

---

_创建于 2026-05-12_
